# portmap-HDL
A lightweight CLI tool to extract and display port definitions from Verilog files. Detects input, output, and inout ports with bus widths, outputs a clean aligned table, and supports Markdown export.
